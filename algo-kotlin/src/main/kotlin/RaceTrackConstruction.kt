class RaceTrackConstruction {

    fun solution(board: Array<IntArray>): Int {
        var answer = 0

        return answer
    }

    /*
    * X, Y 중 하나가 1씩 값이 변하는게 가능
    * 만약 이전 조건에서 X 가 변했는데 이번에는 Y가 변한다면 2 증가
    * */

}

fun main() {

    val board = arrayOf(
        intArrayOf()
    )

    println(RaceTrackConstruction().solution(board))
}